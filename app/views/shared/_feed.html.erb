<% if @feed_items.any? %>
  <ol class="microposts">
    <!--the partial will be inserted once for each member inside the collection-->
    <%= render partial: 'shared/feed_item', collection: @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>